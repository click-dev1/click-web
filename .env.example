# HubSpot — contact questionnaire (see docs/HUBSPOT_SETUP.md).
# Both values are public: they ship in the markup of every embedded HubSpot
# form. Leave them unset and the modal runs in preview mode — it works, it
# just doesn't store anything.

HUBSPOT_PORTAL_ID=
HUBSPOT_FORM_GUID=
