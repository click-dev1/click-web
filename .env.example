# Absolute site origin, no trailing slash — e.g. https://clickmedia.group
# Resolves canonical + Open Graph URLs, robots.txt, sitemap.xml and the
# JSON-LD @ids. Optional on Vercel (falls back to the project's production
# domain); required on any other host. See lib/site.ts.
NEXT_PUBLIC_SITE_URL=
